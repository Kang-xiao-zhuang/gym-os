// Placeholder smoke test. Real widget tests will need Supabase mocked, so the
// generated MyApp test was removed when the app was wired to Supabase.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
